# lib-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build lib-plugin` to build the library.

## Running unit tests

Run `nx test lib-plugin` to execute the unit tests via [Vitest](https://vitest.dev/).
